/**
 * Chinese (Simplified) strings for small items that belong to no larger feature (PWA, client preferences and so on).
 * Keys are prefixed by UI area, as in `core.ts`; `as const` keeps each key a
 * literal so `t()` can check it.
 */
export const misc = {
  "sound.blockNoTalkPower": "你在这个频道没有发言权，音效不会发送到频道。可以用 🎧 自己试听。",
} as const;
