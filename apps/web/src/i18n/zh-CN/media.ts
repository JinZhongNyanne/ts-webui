/**
 * Chinese (Simplified) strings for file transfers, media streaming and the viewer.
 * Keys are prefixed by UI area, as in `core.ts`; `as const` keeps each key a
 * literal so `t()` can check it.
 */
export const media = {
  "media.notMedia": "只有视频和音频文件才能在线播放",
} as const;
